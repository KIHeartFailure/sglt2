```{r outtab, cache=cacheon}
survmy <- function(time, event, eventname, data, matched = FALSE,
                   sensanalysis = FALSE, fg = FALSE) {
  tmpdata <- data

  if (sensanalysis) {
    tmpnrow <- 1
  } else {
    tmpnrow <- 7
  }

  out <- data.frame(matrix(NA, ncol = 4, nrow = tmpnrow))

  out[1, 1] <- eventname
  colnames(out) <- c("Outcome", "Model", "SGLT2 No", "SGLT2 Yes")

  if (!sensanalysis) {
    ## mort (from survfit)
    fit <- survfit(formula(paste0("Surv(", time, ",", event, "=='Yes') ~ ddr_sglt2")),
      data = tmpdata
    )

    sfit <- summary(fit, times = c(30, 60, 90, 365))

    if (length(sfit$surv) != 8) {
      stop("not enough follow-up time for all time points, CHECK!!!!")
    }

    out[1:4, 2] <- paste("% (95% CI)", c(30, 60, 90, 365), "mo")

    out[1:4, 3:4] <- paste0(
      dF(100 - sfit$surv * 100, dig = 0), " (",
      dF(100 - sfit$upper * 100, dig = 0), "-",
      dF(100 - sfit$lower * 100, dig = 0), ")"
    )

    ## incidence rate
    out[5, 2] <- "Incidence (no events, sum py, rate/1000py (95% CI))"

    tmpdata$eventcount <- tmpdata[, event] == "Yes"
    tmpdata <- tmpdata %>% mutate(eventcount = ifelse(eventcount, 1, 0))


    ev <- by(tmpdata$eventcount, tmpdata[, "ddr_sglt2"], sum)
    s <- by(tmpdata[, time], tmpdata[, "ddr_sglt2"], sum) / 365.25
    r <- pois.exact(x = ev, pt = s / 1000)

    out[5, 3:4] <- paste0(
      ev, ", ",
      dF(s, dig = 0), ", ",
      dF(r$rate, dig = 0), " (",
      dF(r$lower, dig = 0), "-",
      dF(r$upper, dig = 0), ")"
    )


    ## cox regressions
    if (!matched) {
      # crude
      mod <- coxph(formula(paste0("Surv(", time, ",", event, "=='Yes') ~ ddr_sglt2")),
        data = tmpdata
      )
      smod <- summary(mod)
      out[6, 2] <- "Crude HR (95% CI), p-value"
      out[6, 3:4] <- c("ref", paste0(
        dF(smod$conf.int[1, 1], dig = 2),
        " (", dF(smod$conf.int[1, 3], dig = 2),
        "-", dF(smod$conf.int[1, 4], dig = 2), "), ",
        dF(smod$coef[1, 5], dig = 3, p = TRUE)
      ))

      # adjusted

      amod <- my.coxph.mids(formula(paste0(
        "Surv(", time, ",", event, " == 'Yes') ~ ddr_sglt2 +",
        paste(modvarsns, collapse = " + ")
      )),
      data = imp
      )

      ## df the number of events minus the regression coefficients.
      ## There is support for this from middle of page 149 of the book by Parmer & Machin (ISBN 0471936405)
      asmod <- summary(pool(amod,
        dfcom =
          (amod$analyses[[1]]$nevent - length(amod$analyses[[1]]$coefficients))
      ))

      out[7, 2] <- "Adj HR (95% CI), p-value"
      out[7, 3:4] <- c("ref", paste0(
        dF(exp(asmod$estimate[1]), dig = 2),
        " (", dF(exp(asmod$estimate[1] - z05 * asmod$std.error[1]), dig = 2),
        "-", dF(exp(asmod$estimate[1] + z05 * asmod$std.error[1]), dig = 2), "), ",
        dF(asmod$p.value[1], dig = 3, p = TRUE)
      ))
    }
  }

  if (matched) {
    if (!fg) {
      mod <- coxme(formula(paste0("Surv(", time, ",", event, "=='Yes') ~ ddr_sglt2 + (1|par)")),
        data = tmpdata
      )

      se <- sqrt(vcov(mod))

      # P-value
      zp <- mod$coeff / se
      p <- dF((1 - pnorm(abs(zp))) * 2, dig = 3, p = TRUE)

      out[tmpnrow, 2] <- "Adj HR (95% CI), p-value"

      out[tmpnrow, 3:4] <- c("ref", paste0(
        dF(exp(mod$coeff), dig = 2),
        " (", dF(exp(mod$coef - z05 * se), dig = 2),
        "-", dF(exp(mod$coef + z05 * se), dig = 2), "), ",
        p
      ))
    }
    if (fg) {
      mod <- summary(z <- crr(tmpdata %>% pull(!!sym(time)),
        tmpdata %>% pull(!!sym(event)),
        tmpdata %>% pull(ddr_sglt2num),
        failcode = 1, cencode = 0
      ))

      # P-value
      p <- dF(mod$coef[, 5], dig = 3, p = TRUE)

      out[tmpnrow, 2] <- "Adj HR (95% CI), p-value"

      out[tmpnrow, 3:4] <- c("ref", paste0(
        dF(mod$conf.int[, 1], dig = 2),
        " (", dF(mod$conf.int[, 3], dig = 2),
        "-", dF(mod$conf.int[, 4], dig = 2), ") ",
        p
      ))
    }
  }
  return(out)
}

empty_func <- function(nrowmy = 3) {
  empty <- data.frame(matrix(NA, nrow = nrowmy, ncol = 4))
  colnames(empty) <- c("Outcome", "Model", "SGLT2 No", "SGLT2 Yes")
  return(empty)
}

survmy2 <- function(time2, event2, eventname2,
                    time2_cross, event2_cross,
                    event2_comp) {
  out1 <- survmy(
    time = time2, event = event2,
    eventname = eventname2,
    data = pdata
  )
  out2 <- survmy(
    time = time2, event = event2,
    eventname = eventname2,
    data = matchp, matched = TRUE
  )
  out3 <- survmy(
    time = time2_cross, event = event2_cross,
    eventname = paste0("Consistency - ", eventname2, " sglt2 crossover censored"),
    data = matchp, matched = TRUE, sensanalysis = TRUE
  )
  out4 <- survmy(
    time = time2, event = event2,
    eventname = paste0("Consistency - ", eventname2, "  excl NT-proBNP"),
    data = matchp_sens, matched = TRUE, sensanalysis = TRUE
  )
  if (!is.null(event2_comp)) {
    out5 <- survmy(
      time = time2, event = event2_comp,
      eventname = paste0("Consistency - ", eventname2, "   death as competing event"),
      data = matchp, matched = TRUE, sensanalysis = TRUE,
      fg = TRUE
    )
  } 

  all <- rbind(out1, empty_func(ifelse(!is.null(event2_comp), 3, 2)))
  if (!is.null(event2_comp)) {match <- rbind(out2, out3, out4, out5)} else{
    match <- rbind(out2, out3, out4)
  }

  outall <- cbind(
    Outcome = coalesce(all[, 1], match[, 1]),
    Model = coalesce(all[, 2], match[, 2]),
    all[3:4],
    match[3:4]
  )
}

cvdeathhfhosp <- survmy2(
  time2 = "sos_outtime_hosphf",
  event2 = "sos_out_deathcvhosphf",
  eventname2 = "CVD/first HF hospitalization",
  time2_cross = "sos_outtime_hosphf_sglt2",
  event2_cross = "sos_out_deathcvhosphf_sglt2",
  event2_comp = "sos_out_deathcvhosphf_comp"
)

death <- survmy2(
  time2 = "sos_outtime_death",
  event2 = "sos_out_death",
  eventname2 = "All-cause mortality",
  time2_cross = "sos_outtime_death_sglt2",
  event2_cross = "sos_out_death_sglt2",
  event2_comp = NULL
)

cvdeath <- survmy2(
  time2 = "sos_outtime_death",
  event2 = "sos_out_deathcv",
  eventname2 = "CV mortality",
  time2_cross = "sos_outtime_death_sglt2",
  event2_cross = "sos_out_deathcv_sglt2",
  event2_comp = "sos_out_deathcv_comp"
)

hfhosp <- survmy2(
  time2 = "sos_outtime_hosphf",
  event2 = "sos_out_hosphf",
  eventname2 = "First HF hospitalization",
  time2_cross = "sos_outtime_hosphf_sglt2",
  event2_cross = "sos_out_hosphf_sglt2",
  event2_comp = "sos_out_hosphf_comp"
)

cvhosp <- survmy2(
  time2 = "sos_outtime_hospcv",
  event2 = "sos_out_hospcv",
  eventname2 = "First CV hospitalization",
  time2_cross = "sos_outtime_hospcv_sglt2",
  event2_cross = "sos_out_hospcv_sglt2",
  event2_comp = "sos_out_hospcv_comp"
)

cvdmis <- survmy2(
  time2 = "sos_outtime_hospmistroketia",
  event2 = "sos_out_deathcvhospmistroketia",
  eventname2 = "CVD/first hospitalization for mi/stroke",
  time2_cross = "sos_outtime_hospmistroketia_sglt2",
  event2_cross = "sos_out_deathcvhospmistroketia_sglt2",
  event2_comp = "sos_out_deathcvhospmistroketia_comp"
)

outall <- rbind(
  cvdeathhfhosp,
  death,
  cvdeath,
  hfhosp,
  cvhosp,
  cvdmis
)


write.xlsx(outall, paste0("./output/tabs/outtab_", Sys.Date(), ".xlsx"), rowNames = FALSE)

myHeader <- c(" " = 1, " " = 1, "All" = 2, "Matched" = 2)
names(myHeader) <- c(" ", " ", "All", "Matched")


mykable(outall,
  fontsize = 6,
  caption = "Outcomes"
) %>%
  landscape() %>%
  add_header_above(myHeader)
```
